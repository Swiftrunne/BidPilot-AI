import { NextResponse } from 'next/server';
import { analyzeSolicitationPdfFiles, analyzeSolicitationText, AnalysisError } from '@/lib/analyze';
import { buildPackageText, extractPdfText } from '@/lib/documents/extract';
import { MAX_FILE_COUNT, validatePdfFile } from '@/lib/documents/validate';
export const runtime='nodejs';
const buckets=new Map<string,{count:number;reset:number}>();
function rateLimit(ip:string){const now=Date.now(); const b=buckets.get(ip); if(!b||b.reset<now){buckets.set(ip,{count:1,reset:now+60_000});return false} b.count++; return b.count>10}
export async function POST(request:Request){const requestId=crypto.randomUUID(); try{ const ip=request.headers.get('x-forwarded-for')?.split(',')[0]?.trim()||'local'; if(rateLimit(ip)) return NextResponse.json({error:'Too many analysis requests. Please wait and try again.',requestId},{status:429}); const form=await request.formData(); const files=form.getAll('file').filter((f):f is File=>f instanceof File); if(!files.length) return NextResponse.json({error:'Upload one or more PDF files.',requestId},{status:400}); if(files.length>MAX_FILE_COUNT) return NextResponse.json({error:`Upload up to ${MAX_FILE_COUNT} PDF files per analysis.`,requestId},{status:400}); const docs=[]; const pdfFiles=[]; for(const file of files){const name=await validatePdfFile(file); const buffer=Buffer.from(await file.arrayBuffer()); pdfFiles.push({name,buffer}); try{docs.push(await extractPdfText(buffer,name));}catch(e){ if(e instanceof Error && e.message.includes('No selectable text')) continue; throw e; }} const analysis=await Promise.race([docs.length?analyzeSolicitationText(buildPackageText(docs),requestId):analyzeSolicitationPdfFiles(pdfFiles,requestId),new Promise((_,rej)=>setTimeout(()=>rej(new AnalysisError('Analysis timed out safely. Try a smaller/OCR-optimized document package.',504)),115000))]) ; return NextResponse.json(analysis,{headers:{'X-Request-Id':requestId}}); }catch(e){const status=e instanceof AnalysisError?e.status: e instanceof Error && e.message.includes('PDF')?400:500; return NextResponse.json({error:e instanceof Error?e.message:'Analysis failed safely.',requestId},{status,headers:{'X-Request-Id':requestId}})} }
