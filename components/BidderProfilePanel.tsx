'use client';
const fields=['Business name','Business location','States/service areas','NAICS codes','Certifications','Licenses','Insurance capability','Bonding capability/capacity','Past performance','Supplier/manufacturer relationships','Delivery capability','Installation capability','Training capability','Set-aside eligibility','Notes'];
export function BidderProfilePanel({profile,setProfile}:{profile:Record<string,string>;setProfile:(profile:Record<string,string>)=>void}){return <div className="space-y-2">{fields.map(f=><input key={f} placeholder={f} value={profile[f]||''} onChange={e=>{const p={...profile,[f]:e.target.value};setProfile(p);localStorage.setItem('bidpilot.profile',JSON.stringify(p))}} className="w-full rounded-lg border p-2 text-sm"/>)}<p className="text-xs text-slate-500">Stored locally in this browser only. Unknown fields become Needs Clarification, not automatic No Bid.</p></div>}
