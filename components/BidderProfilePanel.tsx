'use client';
const fields=['Business name','Business location','States/service areas','NAICS codes','Certifications','Licenses','Insurance capability','Bonding capability/capacity','Past performance','Supplier/manufacturer relationships','Delivery capability','Installation capability','Training capability','Set-aside eligibility','Notes'];
export function BidderProfilePanel({profile,setProfile}:{profile:Record<string,string>;setProfile:(profile:Record<string,string>)=>void}){return <div className="space-y-3">{fields.map(f=><label key={f} className="block"><span className="text-xs font-black uppercase tracking-[0.14em] text-slate-500">{f}</span><input placeholder={`Enter ${f.toLowerCase()}`} value={profile[f]||''} onChange={e=>{const p={...profile,[f]:e.target.value};setProfile(p);localStorage.setItem('bidpilot.profile',JSON.stringify(p))}} className="mt-1 w-full rounded-xl border border-slate-200 bg-slate-50 p-3 text-sm outline-none transition focus:border-aqua focus:bg-white focus:ring-2 focus:ring-aqua/20"/></label>)}<p className="rounded-xl bg-cyan-50 p-3 text-xs text-slate-600">Stored locally in this browser only. Unknown fields become Needs Clarification, not automatic No Bid.</p></div>}
